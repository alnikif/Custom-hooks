import {FC} from "react";
import style from './Title.module.scss'

export const Title = () => {
    return (
        <div className={style.titleWrapper}>
         Custom Hooks();
        </div>
    )
}